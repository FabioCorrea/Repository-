cd descascador-backend
docker-compose up -d
# aguarde PostgreSQL subir
npm install
npm run migrate   # aplica migrations
npm run seed      # popula dados de exemplo (usuário fds.correa@gmail.com)
npm start         # inicia API em http://localhost:4000
